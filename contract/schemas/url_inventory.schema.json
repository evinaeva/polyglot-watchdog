{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://polyglot-watchdog/contract/schemas/url_inventory.schema.json",
  "title": "URL Inventory",
  "description": "Phase 0 canonical URL inventory for a base domain (EN). Canonicalization includes scheme=https and fragment removal; additional drops are controlled by url_rules.",
  "type": "array",
  "items": {
    "type": "string",
    "format": "uri"
  },
  "uniqueItems": true
}